%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      %button.navbar-toggle.collapsed(type="button" data-toggle="collapse" data-target="#bs-example-navbar-collapse-3")
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand(href="#{root_path}")
        #{icon('chain-broken', style: "color: white")} dbtc.works
    .collapse.navbar-collapse(id="bs-example-navbar-collapse-3")
      - if logged_in?
        %form.navbar-form.navbar-right(action="#{logout_path}" method="get")
          %button.btn.btn-default(type="submit") Logout
      - else
        %form.navbar-form.navbar-right(action="#{login_path}" method="get")
          %button.btn.btn-default(type="submit") Login
